// Cargo is Ruts's build systema and package manager
// Are used to manage Rust projects bacause it builds the code, download libraries that the code depends ond and build those libraries
fn main() {
    println!("Hello, world!");
}
